version: 0.2

phases:
  install:
    runtime-versions:
      docker: 20

  build:
    commands:
      - echo "Construyendo imagen Docker..."
      - docker build -t flask-app ./app

artifacts:
  files:
    - '**/*'